# คู่มือการเปิดใช้งานระบบซิงค์ข้อมูลคลาวด์ (Cloud Sync Setup Guide)
### สำหรับแอปบันทึกเวลาทำงาน TimeTrack Pro

เพื่อให้ข้อมูลการลงเวลาเข้า-ออกงาน, รูปถ่ายใบหน้า, และพิกัด GPS **ซิงค์กันแบบเรียลไทม์ระหว่างมือถือของพนักงานทุกคนกับคอมพิวเตอร์ของผู้บริหาร** ท่านสามารถเปิดใช้งานฐานข้อมูลคลาวด์ฟรีจาก Google Firebase ได้ตามขั้นตอนด้านล่างนี้ (ใช้เวลาประมาณ 2-3 นาที ไม่มีค่าใช้จ่าย)

---

## ขั้นตอนที่ 1: สร้างฐานข้อมูลบน Google Firebase (ฟรี 100%)

1. เข้าไปที่เว็บไซต์: **[https://console.firebase.google.com/](https://console.firebase.google.com/)**
2. ล็อกอินด้วยบัญชี Gmail ของท่าน
3. กดปุ่ม **"Add project"** (เพิ่มโปรเจกต์)
   - ตั้งชื่อโปรเจกต์ เช่น `timetrack-company` แล้วกด Continue จนสร้างเสร็จ
4. ที่เมนูด้านซ้าย ไปที่หัวข้อ **Build** -> เลือก **Realtime Database**
5. กดปุ่ม **"Create Database"** (สร้างฐานข้อมูล)
   - ตำแหน่งที่ตั้ง (Location): แนะนำเลือก `Singapore (asia-southeast1)` เพื่อให้เชื่อมต่อเร็วที่สุดในไทย
   - กฎความปลอดภัย (Security Rules): เลือก **"Start in test mode"** (เริ่มต้นในโหมดทดสอบ) แล้วกด Enable

---

## ขั้นตอนที่ 2: นำค่าการเชื่อมต่อมาใส่ในระบบ

1. ที่เมนู Realtime Database ท่านจะเห็น URL ฐานข้อมูลที่ด้านบน เช่น:
   ```
   https://timetrack-company-default-rtdb.asia-southeast1.firebasedatabase.app/
   ```
2. ไปที่รูปฟันเฟือง ⚙️ มุมบนซ้าย -> เลือก **Project settings**
3. เลื่อนลงมาด้านล่างที่หัวข้อ **"Your apps"** -> กดไอคอนเว็บ **`</>` (Web)**
4. ตั้งชื่อแอป เช่น `TimeTrack Web` แล้วกด **Register app**
5. ท่านจะเห็นโค้ด `firebaseConfig` คล้ายตัวอย่างนี้:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyD-xxxxxx...",
  authDomain: "timetrack-company.firebaseapp.com",
  databaseURL: "https://timetrack-company-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "timetrack-company",
  storageBucket: "timetrack-company.appspot.com",
  messagingSenderId: "1234567890",
  appId: "1:1234567890:web:xxxxxx"
};
```

---

## ขั้นตอนที่ 3: บันทึกค่าลงในโปรเจกต์ (เลือกวิธีใดวิธีหนึ่ง)

### วิธีที่ A: กรอกผ่านหน้าเว็บโดยตรง (ง่ายที่สุด)
1. เปิดหน้าเว็บ `index.html` บนคอมพิวเตอร์
2. กดปุ่มไอคอน **⚙️ (ตั้งค่า)** ที่มุมขวาบน
3. ไปที่แท็บ **"☁️ ตั้งค่าคลาวด์ (Cloud Sync)"**
4. วางโค้ดหรือค่า Firebase Config ลงในช่อง แล้วกดปุ่ม **"บันทึกและเชื่อมต่อคลาวด์"**
5. สถานะมุมบนจะเปลี่ยนเป็นสีเขียว **🟢 คลาวด์ออนไลน์ (ซิงค์ทุกเครื่อง)** ทันที!

### วิธีที่ B: ใส่ในไฟล์ `cloud-config.js` (แนะนำเมื่อนำขึ้นเว็บโฮสติ้ง)
1. เปิดไฟล์ `cloud-config.js` ด้วยโปรแกรมแก้ไขข้อความ (เช่น Notepad หรือ VS Code)
2. วางค่าคอนฟิกแทนที่ในช่องว่าง
3. อัปโหลดไฟล์ทั้งหมดขึ้นเว็บโฮสติ้ง (เช่น Vercel, Netlify, GitHub Pages)
4. เมื่อพนักงานทุกคนเปิดเว็บจากลิงก์บนมือถือ **ทุกคนจะเชื่อมต่อฐานข้อมูลเดียวกันทันทีอัตโนมัติ โดยไม่ต้องตั้งค่าอะไรเพิ่มเลยครับ!**

---

## 🎉 ผลลัพธ์เมื่อระบบเชื่อมต่อคลาวด์สำเร็จ
- พนักงานกดเข้างานจากมือถือหน้าไซต์งาน -> หน้าจอคอมพิวเตอร์ที่ออฟฟิศจะเด้งรายชื่อและรูปถ่ายขึ้นมาทันทีใน 1 วินาที!
- ผู้บริหารเพิ่มหรือลบรายชื่อพนักงานจากคอมพิวเตอร์ -> รายชื่อในมือถือของทุกคนจะอัปเดตทันที
- ประวัติและสถิติทั้งหมดจะรวมศูนย์อยู่ที่คลาวด์ ปลอดภัยและไม่สูญหาย
