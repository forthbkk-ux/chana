# คู่มือการเปิดใช้งาน Google Sheets เป็นฐานข้อมูล (Google Sheets as Database)
### สำหรับระบบลงเวลา TimeTrack Pro

การใช้ Google Sheets เป็นฐานข้อมูล จะทำให้ข้อมูลการลงเวลาเข้า-ออกงาน, รูปถ่าย และพิกัด GPS วิ่งเข้าไปบันทึกในตาราง Excel/Google Sheets ของท่านโดยตรง และทุกอุปกรณ์ (มือถือ, ไอแพด, คอมพิวเตอร์) จะซิงค์ข้อมูลตรงกัน 100% 

---

## 3 ขั้นตอนง่ายๆ ในการเชื่อมต่อ Google Sheets:

### ขั้นตอนที่ 1: สร้าง Google Sheet ใหม่
1. เปิดเบราว์เซอร์แล้วไปที่: **[https://sheets.new](https://sheets.new)** เพื่อสร้างสเปรดชีตเปล่าใหม่ 1 ไฟล์
2. ตั้งชื่อไฟล์ตามต้องการ เช่น `ระบบลงเวลาพนักงาน 2026`

---

### ขั้นตอนที่ 2: วางโค้ด Apps Script
1. ที่เมนูด้านบนของ Google Sheet ให้คลิกที่ **"ส่วนขยาย" (Extensions)** -> เลือก **"Apps Script"**
2. ลบโค้ดเก่าในหน้าต่างนั้นออกทั้งหมด
3. เปิดไฟล์ **[`google-apps-script.js`](file:///c:/Users/chana/Desktop/บันทึกเวลา/google-apps-script.js)** ในโฟลเดอร์โปรเจกต์นี้ คัดลอกโค้ดทั้งหมดมาวางลงไป
4. กดรูปแผ่นดิสก์ 💾 เพื่อ **บันทึก (Save)** โครงการ

---

### ขั้นตอนที่ 3: ทำให้ใช้งานได้ (Deploy as Web App)
1. ที่มุมขวาบน ให้คลิกปุ่มสีน้ำเงิน **"การทำให้ใช้งานได้" (Deploy)** -> เลือก **"การทำให้ใช้งานได้รายการใหม่" (New deployment)**
2. คลิกที่รูปฟันเฟือง ⚙️ ด้านซ้าย -> เลือกประเภทเป็น **"เว็บแอปพลิเคชัน" (Web app)**
3. ตั้งค่าตามนี้ (สำคัญมาก):
   - **คำอธิบาย (Description):** ใส่ว่า `TimeTrack API`
   - **เรียกใช้ในฐานะ (Execute as):** เลือก **ฉัน (บัญชีอีเมลของคุณ)**
   - **ผู้มีสิทธิ์เข้าถึง (Who has access):** เลือก **ทุกคน (Anyone)** *(เพื่อให้มือถือพนักงานส่งข้อมูลได้)*
4. กดปุ่ม **"การทำให้ใช้งานได้" (Deploy)**
   - หากมีหน้าต่างขึ้นมาขออนุญาต (Authorization required) ให้กด **"ตรวจสอบสิทธิ์" (Review permissions)** -> เลือกบัญชี Gmail ของคุณ -> กด **"ขั้นสูง" (Advanced)** -> กด **"ไปที่ TimeTrack API (ไม่ปลอดภัย)"** -> กด **"อนุญาต" (Allow)**
5. ท่านจะได้รับ **URL ของเว็บแอปพลิเคชัน (Web app URL)** หน้าตาประมาณนี้:
   ```
   https://script.google.com/macros/s/AKfycbx.../exec
   ```
   ให้กดปุ่ม **"คัดลอก" (Copy)** URL นั้นไว้

---

## ขั้นตอนที่ 4: นำ URL มาใส่ในระบบ TimeTrack Pro

ทำได้ 2 วิธี (เลือกวิธีใดวิธีหนึ่ง):

### วิธีที่ A: กรอกผ่านหน้าเว็บ (เร็วที่สุด)
1. เปิดหน้าเว็บแดชบอร์ด `index.html` (หรือเปิดบน Vercel)
2. กดไอคอนก้อนเมฆ **☁️ (ตั้งค่าคลาวด์/Google Sheets)** ที่มุมบนขวา
3. นำ URL ที่คัดลอกมาวางในช่อง **"Google Sheets Web App URL"** แล้วกดปุ่ม **"บันทึกและเชื่อมต่อ Google Sheets"**
4. หน้าเว็บจะซิงค์ข้อมูลกับ Google Sheet ทันที!

### วิธีที่ B: ใส่ในไฟล์ `cloud-config.js` (แนะนำเมื่อเอาขึ้น Vercel)
1. เปิดไฟล์ [`cloud-config.js`](file:///c:/Users/chana/Desktop/บันทึกเวลา/cloud-config.js)
2. วาง URL ลงในตัวแปร `GOOGLE_SHEET_URL`:
   ```javascript
   window.GOOGLE_SHEET_URL = "https://script.google.com/macros/s/AKfycbx.../exec";
   ```
3. นำไฟล์ขึ้น Vercel ทุกเครื่องในบริษัทจะเชื่อมต่อ Google Sheet เดียวกันทันที 100%!

---

## โครงสร้างตารางใน Google Sheet (แผ่นงาน: "รายชื่อพนักงาน"):

แผ่นงาน **"รายชื่อพนักงาน"** มี 4 คอลัมน์ดังนี้ (คอลัมน์ D คือ **password** ตามรูปที่วงไว้):

| คอลัมน์ A | คอลัมน์ B | คอลัมน์ C | คอลัมน์ D |
| :--- | :--- | :--- | :--- |
| **รหัสพนักงาน** | **ชื่อ - นามสกุล** | **แผนก** | **password** |
| `1` | ปอนด์ รังสิต | ฝ่ายไอทีและระบบ | `1234` |
| `B001` | นุ๊ค พระรามสอง | DOPA | `1234` |
| `N001` | ทัศน์ พระรามสอง | AI | `1234` |
| `R001` | วิทย์ วาริน | AI | `1234` |

> 💡 **วิธีทำให้รหัสผ่านแสดงในช่อง password (คอลัมน์ D) ทันที:**
> - **วิธีที่ 1 (ผ่านหน้าเว็บ):** กดปุ่มก้อนเมฆ ☁️ (มุมขวาบน) -> กดปุ่ม **"ส่งข้อมูลในเครื่องขึ้น Google Sheets"** รหัสผ่านทั้งหมดจะถูกส่งไปกรอกในคอลัมน์ D ทันที (หรือเพียงแค่เปิด/รีเฟรชหน้าเว็บ ระบบจะตรวจจับและส่งรหัสผ่านขึ้นชีทให้อัตโนมัติ)
> - **วิธีที่ 2 (ทำใน Google Sheet):** ในหน้า Google Sheet จะมีเมนูด้านบนชื่อ **"⚡ TimeTrack"** -> ให้คลิก **"🔑 แสดง/เติมรหัสผ่านในช่อง password (คอลัมน์ D)"** รหัสผ่านจะถูกเติมลงในแถวที่ว่างอยู่ทันที
